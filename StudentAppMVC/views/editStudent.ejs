<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
        <title>Student List App</title>
    </head>
<body>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
        <div class="container-fluid">
          <a class="navbar-brand" href="/">Student List App</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="collapsibleNavbar">
            <ul class="navbar-nav">
              <li class="nav-item">
                <a class="nav-link" href="/addStudent">Add new student</a>
              </li> 
            </ul>
          </div>
        </div>
    </nav>

    <div class="container">
        <br>
        <div class="text-center"><h2>Update Student Details</h2></div>
        <br>
        <div class="text-center">
          <% if (student) { %> 
            <form action="/editStudent/<%= student.studentId %>" method="POST" enctype="multipart/form-data">
                <label for="name">Student Name:</label><br>
                <input type="text" id="name" name="name" value="<%= student.name %>" required><br><br>

                <% const dob = new Date(student.dob); %>
                <% const day = String(dob.getDate()).padStart(2, '0'); %>
                <% const month = String(dob.getMonth() + 1).padStart(2, '0'); %>
                <% const year = dob.getFullYear(); %>
                <% const formattedDob = `${year}-${month}-${day}`; %>

                <label for="dob">D.o.B:</label><br>
                <input type="date" id="dob" name="dob" value="<%= formattedDob %>" required><br><br>

                <label for="contact">Contact:</label><br>
                <input type="number" id="contact" name="contact" value="<%= student.contact %>" required><br><br>

                <input type="hidden" name="currentImage" value="<%= student.image %>">
                <label>Current Image:</label><br>
                <% if (student.image) { %>
                  <img src="/images/<%= student.image %>" width="20%" alt="student image"><br><br>
                <% } %>

                <label for="image">New Image (optional):</label><br>
                <input type="file" id="image" name="image" accept="image/*"><br><br>

                <button type="submit" class="btn btn-primary">Update Student</button>
            </form>
          <% } else { %>
              <p>No Student found.</p>
          <% } %>
          <a href ="/">Back</a>
        </div>
    </div>
</body>
</html>
